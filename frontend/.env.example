# Local dev only — frontend (port 5173) and backend (port 8000) run separately.
VITE_API_URL=http://localhost:8000

# In production (single Vercel project, frontend + /api on the same domain via
# vercel.json rewrites), leave VITE_API_URL unset entirely so API calls resolve
# to relative same-origin paths instead.
